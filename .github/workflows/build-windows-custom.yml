name: Build Custom Windows Installer

on:
  workflow_dispatch:

jobs:
  build-windows:
    uses: ./.github/workflows/flutter-build.yml
    with:
      upload-artifact: true
      upload-tag: custom-windows
